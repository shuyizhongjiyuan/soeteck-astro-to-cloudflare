---
interface Props {
  eyebrow: string;
  title: string;
  text?: string;
  id?: string;
}

const { eyebrow, title, text, id } = Astro.props;
---
<div class="about-section-heading">
  <p class="soeteck-eyebrow">{eyebrow}</p>
  <h2 id={id}>{title}</h2>
  {text && <p>{text}</p>}
</div>

<style>
  .about-section-heading {
    max-width: 820px;
    margin-bottom: clamp(32px, 4vw, 48px);
  }

  .about-section-heading h2 {
    margin: 0;
    color: var(--soeteck-text);
  }

  /* 6.1 标准 Eyebrow — 基类样式 + 间距覆盖（全局基类 20px → 规范 25px） */
  .about-section-heading .soeteck-eyebrow {
    margin: 0 0 25px;
    padding: 0;
  }

  .about-section-heading p:not(.soeteck-eyebrow) {
    margin: 20px 0 0;
    color: var(--soeteck-text-soft);
    font-size: var(--soeteck-text-md);
    line-height: 1.7;
  }
</style>
